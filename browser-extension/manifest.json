{
  "manifest_version": 3,
  "name": "Local Academic Research Workbench — Capture",
  "version": "0.1.0",
  "description": "Save the current page (DOI / URL / title) to your local research workbench. Minimal by design: one action.",
  "permissions": ["activeTab", "storage"],
  "host_permissions": ["http://127.0.0.1:8000/*", "http://localhost:8000/*"],
  "action": {
    "default_popup": "popup.html",
    "default_title": "Save to Local Workbench"
  },
  "background": {
    "service_worker": "background.js"
  }
}
